# `ls`

List tasks

```
Usage: 
  elk ls [flags]

Flags:
    -a, --all               Display all the properties from a task
    -f, --file              Specify which file to use 
    -g, --global            Force to use the global file
```